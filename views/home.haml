- if current_user
  %h2= 'Welcome ' + @current_user.first_name

%h1 Home Latest Posts

- @posts.each do |post| 
  .post{:id => "post#{post.id}"}
    %p
      %a{href: "/posts/#{post.id}"}
        = post.title
      #postedby
      by:
      %a{href: "/user/#{post.user.id}"}
        = post.user.username    
  %br 
%br